# 1.7.0 Warehouse Module — Обзор и навигация

## Спецификация модуля

* **Модуль:** Warehouse Module (склад)
* **Тип:** раздел внутренней админки клиники
* **Платформа:** веб-приложение медицинской системы
* **Основные роли:** Кладовщик / сотрудник склада, Заведующий складом, Сотрудник потребительского отдела (инициатор запроса на пополнение)
* **Статус документа:** Актуализировано

> Этот файл — навигационная и паттерн-сводка модуля, по аналогии с `1.6.0. Overview` HR Module: фиксирует общие UX-правила, на которые ссылаются страницы 1.7.15–1.7.23 без повторного описания. Техническая реализация — в `1.7.0. Overview` (Frontend).

> ⚠️ **Файлы `1.7.1`–`1.7.14`, `1.7.0.1` в этой же папке — устаревшая версия дизайна** (написана до текущей ревизии `WarehouseModule.md`, компактный reference-стиль без Wireframes/States/AC). Не использовать как источник истины — актуальные страницы модуля пронумерованы `1.7.0.2` и `1.7.15`–`1.7.23`.

---

## Состав пакета документов

| Страница | Маршрут | Содержание | Статус |
|----------|---------|------------|--------|
| `1.7.0.2. Warehouse Overview` | — | Навигация, общие UI-паттерны | — |
| `1.7.15. Suppliers Page` | `/warehouse/suppliers` | Поставщики, контакты, договоры, номенклатура, долги | Active |
| `1.7.16. Product Catalog Page` | `/warehouse/products` | Каталог товаров, категории, остатки | Active |
| `1.7.17. Supply Orders Page` | `/warehouse/supply-orders` | Заказы на поставку | Active |
| `1.7.18. Receiving Page` | `/warehouse/supplies/:id` | Приёмка поставки: партии, инспекции, коды маркировки | Active |
| `1.7.19. Marking Codes Registry Page` | `/warehouse/marking-codes` | Реестр кодов маркировки, история движения, списание | Active |
| `1.7.20. Storages Page` | `/warehouse/storages` | Склады и остатки | Active |
| `1.7.21. Storage Requests Page` | `/warehouse/storage-requests` | Запросы на пополнение потребительских складов | Active |
| `1.7.22. Inventory Assets Page` | `/warehouse/items` | ТМЦ и их закрепление | Active |
| `1.7.23. Revisions Page` | `/warehouse/revisions` | Ревизии остатков | Active |

Приёмка (`1.7.18`) — не пункт бокового меню, а вложенный экран, открываемый из карточки заказа на поставку (`1.7.17`) кнопкой «Инициировать приёмку» или из списка текущих приёмок.

---

## Карта навигации модуля

```
┌────────────────────────────────────────────────────────────┐
│  Боковое меню → Склад                                       │
│                                                                │
│   ├─ Поставщики              → 1.7.15                        │
│   ├─ Каталог товаров         → 1.7.16                        │
│   ├─ Заказы на поставку      → 1.7.17 ──► Приёмка (1.7.18)   │
│   ├─ Реестр маркировки       → 1.7.19                        │
│   ├─ Склады                  → 1.7.20                        │
│   ├─ Запросы на пополнение   → 1.7.21                        │
│   ├─ ТМЦ                     → 1.7.22                        │
│   └─ Ревизии                 → 1.7.23                        │
└────────────────────────────────────────────────────────────┘
```

---

## Общие UI-паттерны

Зафиксированы один раз; страницы ссылаются на них без повторного описания. Базовые паттерны реестра/формы/массового выбора/контекстного меню/пустых состояний — идентичны `1.6.0. Overview` HR Module (не дублируются здесь дословно, см. там).

### Паттерн «Первичный документ склада»

Часть складских действий (подтверждение заказа на поставку, финализация приёмки, выдача по запросу на пополнение, завершение ревизии) может требовать привязки сопроводительного документа ЭДО — в отличие от HR, где документ прикладывается **при создании** движения, здесь документ прикладывается к уже существующему действию отдельным шагом «Прикрепить документ» (`WarehouseModule.md` §4.22, §12.10). Обязательность условна и определяется настройкой Management Module (`warehouse.primary_document_required`), а не отделом исполнителя, как в HR, — она либо включена для всей клиники, либо выключена для всех четырёх действий сразу.

```
┌────────────────────────────────────────────────┐
│  Подтвердить заказ                              │
├────────────────────────────────────────────────┤
│  ⚠ Требуется сопроводительный документ          │
│                                                   │
│  ┌── Сопроводительный документ ─────────────────┐│
│  │ ◉ Создать новый   ○ Из шаблона               ││
│  │ ○ Загрузить файл  ○ Из входящих              ││
│  ├────────────────────────────────────────────── ┤│
│  │ 📄 Накладная-основание · Draft   [Заменить]  ││
│  └────────────────────────────────────────────── ┘│
├────────────────────────────────────────────────┤
│              [Отмена]      [Подтвердить]         │
└────────────────────────────────────────────────┘
```

Правила:

* Если настройка выключена — кнопка действия («Подтвердить», «Завершить приёмку», «Выдать», «Завершить ревизию») сразу выполняет действие без модалки документа.
* Если включена — модалка документа обязательна для этих четырёх действий; кнопка неактивна без привязанного документа.
* Прикреплённый документ **не означает**, что действие уже применилось — эффект (смена статуса, движение кодов маркировки, списание) наступает только после подписания/доставки документа (`WarehouseModule.md` §3.11-аналог, `EDMModule.md` §3.7) — см. паттерн «Связанные документы» ниже.

### Паттерн «Связанные документы» (отложенные складские действия)

Аналог одноимённого паттерна HR Module (`1.6.0. Overview`), но источник — `warehouse_document_events` (`WarehouseModule.md` §4.22, §12.10), а не `hr_document_events`. На страницах заказов на поставку, приёмки, запросов на пополнение и ревизий под шапкой сущности показывается блок со списком привязанных документов и статусом действия:

```
┌── Связанные документы ──────────────────────────────┐
│  📄 Накладная-основание                              │
│     ● Ожидает подписания              [Открыть →]   │
└───────────────────────────────────────────────────────┘
```

Три состояния (та же цветовая семантика, что и в HR): **Ожидает подписания** (жёлтый), **Применено** (зелёный), **Отменено** (серый). Клик — карточка документа (модуль 1.5). Блок скрыт при отсутствии связанных движений.

### Статус-бейджи

| Семантика | Где используется | Цвет |
|-----------|------------------|------|
| Активно / в порядке | Товар активен, склад активен, код `in_stock`, поставщик `active` | Зелёный |
| В процессе / внимание | Заказ `confirmed`/не отгружен, приёмка `in_progress`, запрос `in_progress`, партия близка к истечению срока (≤30 дней) | Жёлтый/янтарный |
| Критично | Партия `expired`, код `defective`, долг поставщика `open`, ревизия с расхождением | Красный |
| Нейтрально / неактивно | Поставщик `suspended`, товар неактивен, код `written_off`/`dispensed`/`returned_to_supplier`, заказ `canceled` | Серый |

### Права доступа — общий принцип

Видимость и доступность элементов интерфейса определяется набором прав пользователя из диапазона `WAREHOUSE_*` (`WarehouseModule.md` §9): право на просмотр раздела скрывает/показывает сам раздел, право на управление — интерактивные элементы внутри него. Полная сводная таблица — во frontend-документе `1.7.0. Overview` (Frontend).

---

## Acceptance criteria — общие (для всех экранов модуля)

* Навигация Warehouse содержит 8 активных пунктов (Поставщики, Каталог товаров, Заказы на поставку, Реестр маркировки, Склады, Запросы на пополнение, ТМЦ, Ревизии); Приёмка — вложенный экран, не пункт меню.
* Все реестры поддерживают поиск/фильтрацию/пагинацию и состояния пусто/загрузка/ошибка.
* Статус-бейджи во всех разделах используют единую цветовую семантику модуля.
* Действия, гейтуемые сопроводительным документом (подтверждение заказа, финализация приёмки, выдача по запросу, завершение ревизии), корректно ведут себя в обоих случаях настройки (документ обязателен/не обязателен) и показывают блок «Связанные документы» при инициированном, но не применённом действии.
* Элементы без прав скрыты или disabled с tooltip «Недостаточно прав».
